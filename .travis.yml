sudo: required

services:
  - docker

language: go
go:
  - tip

before_install:
  - sudo apt-get -qq update
  - curl https://glide.sh/get | sh
  - docker pull fedora
  - docker run fedora /bin/sh -c "pwd & ls -al"
  - docker run fedora /bin/sh -c "pwd & ls -al"

install:
  - glide install

script: echo "pass"
